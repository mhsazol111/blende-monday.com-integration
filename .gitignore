node_modules/
dist/
*.log

# Secrets & local config
.env
.env.local
.env.*.local

# Local SQLite database files
*.sqlite
*.sqlite3
*.db
data/

# Live ruleset — runtime state written by the configurator, per-server and often
# with real data. Track config/rules.example.json instead (sanitized reference).
config/rules.json
config/rules.*.json

# OS / editor
.DS_Store
.vscode/
.idea/

# WordPress intake bridge — lives on the wordpress-intake-bridge branch only,
# never on main and never in the service's Docker image.
page-monday-intake-doc-2.php
intake-form-field-mapping.txt
